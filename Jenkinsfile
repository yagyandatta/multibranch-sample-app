pipeline {
  agent {label 'built_in'}
  stages {
    stage('Build') {
      steps {
        sh 'echo "build..." '
      }
    }
    stage('Deploy') {
      steps {
        sh 'echo "Deploy..." '
      }
    }
  }
}
